export default function PlaceholderPage({ title }) {
  return (
    <div className="page-card">
      <p className="eyebrow">TaskFlow</p>
      <h1>{title}</h1>
      <p>This screen will be connected to the TaskFlow backend in its dedicated implementation phase.</p>
    </div>
  );
}
